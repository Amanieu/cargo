initSidebarItems({"fn":[["resolve_pkg","Resolve all dependencies for the specified `package` using the previous lockfile as a guide if present.This function will also write the result of resolution as a new lockfile."],["resolve_with_previous","Resolve all dependencies for a package using an optional previous instance of resolve to guide the resolution process.This also takes an optional hash set, `to_avoid`, which is a list of package ids that should be avoided when consulting the previous instance of resolve (often used in pairings with updates).The previous resolve normally comes from a lockfile. This function does not read or write lockfiles from the filesystem."]]});